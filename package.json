{
  "name": "testchamber",
  "version": "1.1.6",
  "description": "utilities for testing",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && npx playwright install && npm link && npm link testchamber",
    "test": "npx c8 --check-coverage npx playwright test",
    "test-nocoverage": "npx playwright test"
  },
  "typings": "index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/iamboobert/testchamber.git"
  },
  "author": "Derek Thompson",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.46.1",
    "@types/node": "^22.5.0"
  }
}
